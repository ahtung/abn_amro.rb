require "spec_helper"

describe AbnAmroRb do
  it "has a version number" do
    expect(AbnAmroRb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
